import XCTest
@testable import MyLibraryPackage

final class MyLibraryPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MyLibraryPackage().text, "Hello, World!")
    }
}
